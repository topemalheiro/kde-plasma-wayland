--- a/cube/contents/ui/CubeCameraController.qml
+++ b/cube/contents/ui/CubeCameraController.qml
@@ -83,7 +83,9 @@
         camera.position = Qt.vector3d(radius * Math.sin(phi) * Math.sin(theta),
                                       radius * Math.cos(theta),
                                       radius * Math.cos(phi) * Math.sin(theta));
-        camera.rotation = root.rotation;
+        // The camera sees the back of the outward-facing cube faces, which
+        // renders upside-down and mirrored left/right. Rotate the camera
+        // 180° around its forward axis to flip the image back to normal.
+        camera.rotation = root.rotation.times(Quaternion.fromEulerAngles(0, 0, 180));
     }
 
     FrameAnimation {
